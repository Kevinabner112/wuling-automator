# See https://firebase.google.com/docs/app-hosting/configure for more information
env:
  - variable: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    value: "your-project-id"
  # Add other production environment variables here or via the Firebase console
